apiVersion: v1
kind: ReplicationController
metadata:
  name: my-rc
  labels:
    app: nginx
spec:
  template:
    metadata:
      name: my-nginx
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx
  replicas: 3
